apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    app: pipeline
  name: pipeline
  namespace: default
spec:
  selector:
    matchLabels:
      app: pipeline
  template:
    metadata:
      labels:
        app: pipeline
    spec:
      containers:
      - image: gcr.io/jk-example-project/pipeline:fe322d4447e54b971a9a75ccd221c964ad90a4ab
        name: pipeline
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
