apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    app: pipeline
  name: pipeline
  namespace: default
spec:
  selector:
    matchLabels:
      app: pipeline
  template:
    metadata:
      labels:
        app: pipeline
    spec:
      containers:
      - image: gcr.io/jk-example-project/pipeline:941e122ea37c68598f660e97d460e1e6a83cccfb
        name: pipeline
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
