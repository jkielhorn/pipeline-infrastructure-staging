apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    app: pipeline
  name: pipeline
  namespace: default
spec:
  selector:
    matchLabels:
      app: pipeline
  template:
    metadata:
      labels:
        app: pipeline
    spec:
      containers:
      - image: gcr.io/jk-example-project/pipeline:5fdad3f22767580c2c9bfc1ea8a1c7ebfdee8b87
        name: pipeline
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
